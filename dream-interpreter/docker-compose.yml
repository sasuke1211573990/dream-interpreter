version: '3'
services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - AI_SERVICE_URL=http://ai-service:5000
    depends_on:
      - ai-service
  ai-service:
    build: ./ai
    ports:
      - "5000:5000"
